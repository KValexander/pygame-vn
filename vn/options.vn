# Переменные экрана, на данные момент не используются

windowsize = (1024, 768)

# Переменные текста

textmargin = 20

textsize = 20

textcolor = (255,255,255)

# Только системные шрифты

textfont = verdana

lineheight = 25
