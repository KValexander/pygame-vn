# Желательно не использовать пробелы между переменной и значением
# Иначе могут быть проблемы с работой кода
# Особенно это касается переменных цветов

# Переменные текста

textmargin=20;

textsize=20;

textcolor=(255,255,255);

# Только системные шрифты

textfont=verdana;

lineheight=25
